# SPDX-License-Identifier: MIT
schema: openautosar.supplier-naming-rules.v1
profile_id: bosch-supplier
status: template-only

rules:
  release_name:
    source: authorized-project-contract
    default_pattern: "{oem}-{program}-{release}"
  software_cluster:
    source: authorized-project-contract
    default_pattern: "{domain}-{cluster}-{version}"
  delivery_id:
    source: authorized-project-contract
    default_pattern: "{project}-{target}-{date}-{sequence}"
